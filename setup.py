import setuptools

setuptools.setup(
    name='retrieval_base', 
    version='0.1', 
    description='Base code for running retrievals of CRIRES+ spectra.', 
    url='#',
    author='samderegt', 
    #install_requires=[''], 
    #author_email='', 
    packages=setuptools.find_packages(), 
    zip_safe=False, 
    )